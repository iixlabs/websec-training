extends layout

block content
	if err
		div.error
			h1 Uh Oh, Error!!
			li= err
			
	if (pages.length == 0)
		h1 Four Oh Four!
		p Page not found :(
	else
		each page in pages
			h1= page.title
			p !{page.content}

	p
		a(href="/sqli/3") Back
